{% set resource = hookable_metadata.context.resource %}

<div class="container-xl mt-3">
    <div class="row">
        <div class="col-12">
            {% set gridView = get_grid_view_by_name(
                'app_form_submission_values',
                {'criteria': {'formSubmission': resource.id}},
                ['formSubmission'],
                ['formSubmission.id']
            ) %}

            {{ sylius_grid_render(gridView) }}
        </div>
    </div>
</div>
